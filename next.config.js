/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domain: [
            'localhost',
            'retro-emporium-production.up.railway.app'
        ]
    }
}

module.exports = nextConfig
