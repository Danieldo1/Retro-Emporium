/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'retro-emporium-production.up.railway.app'
        ]
    }
}

module.exports = nextConfig
